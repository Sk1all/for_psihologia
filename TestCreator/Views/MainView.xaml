<UserControl x:Class="TestCreator.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:TestCreator.ViewModels"
             xmlns:views="clr-namespace:TestCreator.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:MainViewModel}"
             d:DesignHeight="250" d:DesignWidth="580">
    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Command="ApplicationCommands.New"/>
                <Separator/>
                <MenuItem Command="ApplicationCommands.Close"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Command="ApplicationCommands.Help"/>
            </MenuItem>
        </Menu>
        <StatusBar  DockPanel.Dock="Bottom">
            <TextBlock>
                <Run Text="Вопросов: "/>
                <Run Text="{Binding ContentFrame[0].QuestionVMs.Count, Mode=OneWay}"/>
            </TextBlock>
        </StatusBar>
        <views:QuestionListView DataContext="{Binding ContentFrame}"
                                Margin="5"/>
    </DockPanel>
</UserControl>
